{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Permissions granted to the single main desktop window. Blueprint is a single-window application; there is deliberately no capability granting the renderer filesystem or shell access — all privileged work stays in the Rust main process, which is the security property the architecture documents promise.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-request-user-attention",
    "dialog:default"
  ]
}
